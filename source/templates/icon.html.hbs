<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>{{name}} - {{options.fontName}}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">

    <link rel="stylesheet" href="../strib-icons.css">

    <style>
        .strib-icon, .strib-icon-svg {
            font-size: 3em;
        }
        .strib-icon.strib-logo {
            font-size: 2.25em !important;
        }
        .strib-icon-svg.strib-logo {
            width: 3.75em;
            font-size: 4em;
        }
        .icon-demo {
            background-color: var(--bs-tertiary-bg);
            background-image: radial-gradient(circle, var(--bs-border-color) 1px, rgba(0, 0, 0, 0) 1px);
            background-size: 1rem 1rem;
            min-height: 6em;
        }
        .strib-icon:hover {
            color: #8dc63f;
        }
        .strib-icon-svg:hover {
            fill: #8dc63f;
        }
        .codepoint {
            text-transform: uppercase;
        }
        footer {
            font-size: 0.85em;
            padding: 2em;
        }
    </style>
</head>
<body>
<div class="container">
    <header role="banner">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb my-4 px-2 py-1 bg-body-tertiary">
                <li class="breadcrumb-item"><a href="../index.html">Icons</a></li>
                <li class="breadcrumb-item active" aria-current="page">{{name}}</li>
            </ol>
        </nav>
    </header>

    <main>
        <h1 class="display-2">{{name}}</h1>
        <h2 class="display-8">Star Tribune Icons</h2>
        <p>The Star Tribune icons is a font-icon and SVG set to manage icons used in Star Tribune projects.</p>
        <div class="row">
            <div class="col-9">
                <section>
                    <div class="row">
                        <div class="col-6">
                            <h3>Webfont</h3>
                            <div class="icon-demo mb-4 border rounded-3 d-flex align-items-center justify-content-center p-3 py-6" role="img" aria-label="{{name}} - font preview">
                                <span class="strib-icon strib-{{name}}"></span>
                            </div>
                        </div>
                        <div class="col-6">
                            <h3>SVG</h3>
                            <div class="icon-demo mb-4 border rounded-3 d-flex align-items-center justify-content-center p-3 py-6" role="img" aria-label="{{name}} - svg preview">
                                <svg class="strib-icon-svg strib-{{name}}">
                                    <use xlink:href="../strib-icons.sprite.svg#strib-{{name}}"></use>
                                </svg>
                            </div>
                        </div>
                    </div>
                </section>

                <section>
                    <div class="row">
                        <h3>Example Usages</h3>
                        <p>
                            All icons and svgs need to be prefixed with <code>strib-</code>, so <code>{{name}}</code>
                            becomes
                            <code>strib-{{name}}</code>.
                        </p>
                    </div>
                    <div class="row">
                        <div class="col-6">
                            <pre class="bg-body-secondary p-3 rounded">&lt;span class="strib-icon strib-{{name}}">&lt;/span></pre>
                        </div>
                        <div class="col-6">
                            <pre class="bg-body-secondary p-3 rounded">&lt;svg class="strib-icon-svg">
    &lt;use xlink:href="#strib-{{name}}">&lt;/use>
&lt;/svg></pre>
                        </div>
                    </div>
                </section>

                <hr />

                <section>
                    <h3>SVG Contents</h3>
                    <pre class="bg-body-secondary p-3 rounded">{{svg}}</pre>
                </section>
            </div>
            <div class="col-3">
                <div class="row">
                    <div class="col-12">
                        <section class="pb-3">
                            <h3>Codepoint</h3>
                            <ul class="list-unstyled list">
                                <li>Unicode: <code>U+<span class="codepoint">{{metadata.codepoint}}</span></code></li>
                                <li>CSS: <code>\<span class="codepoint">{{metadata.codepoint}}</span></code></li>
                                <li>US: <code>\u<span class="codepoint">{{metadata.codepoint}}</span></code></li>
                                <li>HTML: <code>&#x<span class="codepoint">{{metadata.codepoint}}</span></code></li>
                            </ul>
                        </section>

                        {{#if metadata.variables}}
                            <section class="pb-3">
                                <h3>Variables</h3>
                                The following variables are available for use with this icon:
                                <ul>
                                    {{#each metadata.variables}}
                                        <li><code>--{{this}}</code></li>
                                    {{/each}}
                                </ul>
                                <p>They can be used when styling the SVG icons in a manner <u>similar</u> to this:</p>
                                <pre class="bg-body-secondary p-3 rounded">
.strib-icon-svg {
    --strib-star-top: red;
    --strib-star-bottom: pink;
}</pre>
                            </section>
                        {{/if}}

                        {{#if metadata.aliases}}
                            <section class="pb-3">
                                <h3>Aliases</h3>
                                This icon was previously available under the following name(s):
                                <ul>
                                    {{#each metadata.aliases}}
                                        <li><code>{{this}}</code></li>
                                    {{/each}}
                                </ul>
                                <p>Though this name has been deprecated, code using this name should still continue to
                                    work.</p>
                                <p>
                                    <strong>NOTE:</strong> If there are variables associated with this icon, any
                                    deprecated versions of
                                    those variable names will continue to work only when used with the deprecated alias.
                                </p>
                            </section>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer role="contentinfo" class="bg-body-tertiary">
        &copy; Star Tribune, Last build: {{options.templateOptions.buildDate}}
    </footer>
</div>
</body>
</html>