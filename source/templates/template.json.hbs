{
  "name": "{{ options.fontName }}",
  "version": "{{ options.templateOptions.pkg.version }}",
  "baseSelector": "{{ options.templateOptions.baseSelector }}",
  "classPrefix": "{{ options.templateOptions.classPrefix }}",
  "classes": [
  {{#each classes}}
    "{{ ../options.templateOptions.classPrefix }}{{ this }}",
  {{/each}}
  ]
}
